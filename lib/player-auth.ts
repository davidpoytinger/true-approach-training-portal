import { cookies } from "next/headers";
import { createHmac, randomBytes, scryptSync, timingSafeEqual } from "crypto";
import { caspioFetch } from "./caspio";
export const ACCOUNTS_TABLE_ID="f246kq";export const PLAYER_ACCESS_TABLE_ID="f1o4u9";export const PLAYERS_TABLE_ID="k2a3fa";const COOKIE_NAME="ta_player_account";const SESSION_SECONDS=60*60*24*30;
type Account={AccountID:number;FirstName:string;LastName:string;Email:string;PasswordHash?:string|null;EmailVerified?:boolean|number|null;IsActive?:boolean|number|null};type Access={AccessID:number;PlayerID:number;AccountID:number;Relationship?:string|null;AccessLevel?:string|null;IsPrimary?:boolean|number|null;IsActive?:boolean|number|null;CanEditProfile?:boolean|number|null;CanAddSessions?:boolean|number|null;CanEditPlayerAddedSessions?:boolean|number|null;CanManageAccess?:boolean|number|null};type Response<T>={data:T[]};
function secret(){return process.env.AUTH_SECRET||process.env.CASPIO_CLIENT_SECRET||"";}function sign(v:string){return createHmac("sha256",secret()).update(v).digest("hex");}export function hashPassword(p:string){const salt=randomBytes(16).toString("hex");return `${salt}:${scryptSync(p,salt,64).toString("hex")}`;}export function verifyPassword(p:string,stored:string){try{const[salt,hash]=stored.split(":");return timingSafeEqual(scryptSync(p,salt,64),Buffer.from(hash,"hex"));}catch{return false;}}
export async function setAccountSession(id:number){const expires=Math.floor(Date.now()/1000)+SESSION_SECONDS,payload=`${id}.${expires}`;(await cookies()).set(COOKIE_NAME,`${payload}.${sign(payload)}`,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:SESSION_SECONDS});}export async function clearAccountSession(){(await cookies()).delete(COOKIE_NAME);}export async function getAccountId(){const raw=(await cookies()).get(COOKIE_NAME)?.value;if(!raw||!secret())return null;const[id,expires,sig]=raw.split("."),payload=`${id}.${expires}`,expected=sign(payload);if(!sig||sig.length!==expected.length||!timingSafeEqual(Buffer.from(sig),Buffer.from(expected))||Number(expires)<Math.floor(Date.now()/1000))return null;const n=Number(id);return Number.isInteger(n)&&n>0?n:null;}
export async function findAccountByEmail(email:string){const escaped=email.toLowerCase().replace(/'/g,"''"),where=encodeURIComponent(`Email='${escaped}'`);const r=await caspioFetch<Response<Account>>(`/tables/${ACCOUNTS_TABLE_ID}/records?select=AccountID,FirstName,LastName,Email,PasswordHash,EmailVerified,IsActive&where=${where}&limit=1`);return r.data?.[0]??null;}export async function getAccount(id:number){const r=await caspioFetch<Response<Account>>(`/tables/${ACCOUNTS_TABLE_ID}/records?select=AccountID,FirstName,LastName,Email,PasswordHash,EmailVerified,IsActive&where=AccountID=${id}&limit=1`);return r.data?.[0]??null;}export async function getPlayerAccess(id:number){const r=await caspioFetch<Response<Access>>(`/tables/${PLAYER_ACCESS_TABLE_ID}/records?select=AccessID,PlayerID,AccountID,Relationship,AccessLevel,IsPrimary,IsActive,CanEditProfile,CanAddSessions,CanEditPlayerAddedSessions,CanManageAccess&where=AccountID=${id}&limit=100`);return(r.data??[]).filter(x=>x.IsActive!==false&&x.IsActive!==0);}
